<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for brand in brands %}
        <url>
            <loc>{{ config('env.webservice.url.web') }}{{ brand.url }}</loc>
        </url>
    {% endfor %}
</urlset>
