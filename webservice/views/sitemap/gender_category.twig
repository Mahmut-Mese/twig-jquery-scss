<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for category in gender_categories %}
    <url>
            <loc>{{ config('env.webservice.url.web') }}{{ category }}</loc>
        </url>
    {% endfor %}
</urlset>
